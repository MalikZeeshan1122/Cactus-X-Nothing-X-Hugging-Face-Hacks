name: ai_medical_flutter
description: AI Medical Assistant - Flutter client (minimal scaffold)
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  http: ^1.5.0
  flutter_dotenv: ^5.0.2
  supabase_flutter: ^1.5.0
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
